# dotnetcore-async-api

.Net Core 3 async API.

# Settings

Change your string connection in the appsettings.json file

# Run

Execute the command:

 > dotnet restore
 
 > dotnet ef database update
 
 > dotnet run
